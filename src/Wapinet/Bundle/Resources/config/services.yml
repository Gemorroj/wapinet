parameters:
    wapinet.controller.paginate.class: Wapinet\Bundle\Controller\PaginateController

services:
    wapinet.paginate.controller:
        class:     "%wapinet.controller.paginate.class%"
        calls:
            - [setContainer, ["@service_container"]]