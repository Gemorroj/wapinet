{% extends "base.html.twig" %}

{% block title %}Лог разработки{% endblock %}
{% block breadcrumbs %}
    {{ wapinet_breadcrumbs() }}
{% endblock %}

{% block content %}
    <p>Последние {{ wapinet_git_log }} комитов в репозиторий сайта.</p>
    <div class="ui-corner-all">
        {% for item in log %}
            <div class="ui-bar ui-bar-a">
                <h3>{{ item.datetimeAuthor|wapinet_datetime }}</h3>
            </div>
            <div class="ui-body ui-body-a">
                <p>{{ item.message.fullMessage }}</p>
            </div>
        {% else %}
            <div class="ui-body ui-body-a">
                <p>Репозиторий пуст.</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}
