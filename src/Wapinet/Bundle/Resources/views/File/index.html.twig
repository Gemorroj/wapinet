{% extends '::base.html.twig' %}

{% block title %}Файлообменник{% endblock %}
{% block breadcrumbs %}
    {{wapinet_breadcrumbs({
    1: {'uri': path('downloads'), 'title': 'Загрузки, развлечения'}
    })}}
{% endblock %}

{% block content %}
    <div data-role="collapsible" data-collapsed="false">
        <h2>Новые файлы</h2>
        <ul data-role="listview">
            <li><a href="{{ path('file_list', {'date': 'yesterday'}) }}">За вчера <span class="ui-li-count">{{ file_count_yesterday() }}</span></a></li>
            <li><a href="{{ path('file_list', {'date': 'today'}) }}">За сегодня <span class="ui-li-count">{{ file_count_today() }}</span></a></li>
        </ul>
    </div>

    <ul data-role="listview" data-inset="true">
        <li><a href="{{ path('file_list') }}">Все файлы <span class="ui-li-count">{{ file_count_all() }}</span></a></li>
        <li><a href="{{ path('file_categories') }}">Категории</a></li>
        <li><a href="{{ path('file_tags') }}">Тэги</a></li>
        <li data-icon="search"><a href="{{ path('file_search') }}">Поиск</a></li>
    </ul>

    <div data-role="collapsible" data-collapsed="false">
        <h2>Комментарии</h2>
        <ul data-role="listview">
            {% for comment in comments %}
                <li data-role="list-divider" class="ui-bar-d">
                    <span>
                        <a class="user" data-icon="user" data-mini="true" data-inline="true" data-role="button" href="{{ path('wapinet_user_profile', {'username': comment.username}) }}">{{ comment.username }}</a>
                    </span>
                    <span class="ui-li-count">{{ comment.createdAt|wapinet_datetime }}</span>
                </li>
                <li>
                    <a href="{{ comment.permalink }}">
                        {{ comment.body|wapinet_bbcode_parse }}
                    </a>
                </li>
            {% else %}
                <li>Комментариев нет.</li>
            {% endfor %}
        </ul>
    </div>

    <ul data-role="listview" data-inset="true">
        <li data-icon="action"><a href="{{ path('file_upload') }}">Загрузить файл</a></li>
        <li data-icon="info"><a href="{{ path('file_statistic') }}">Статистика</a></li>
        <li data-icon="alert"><a href="{{ path('file_information') }}">Информация</a></li>
    </ul>
{% endblock %}
