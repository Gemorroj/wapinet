{% extends "::base.html.twig" %}

{% block title %}Регистрация ICQ номера (UIN){% endblock %}

{% block content %}
    {% if result is not null %}
        ...
    {% else %}
        <p>
            Используйте данный сервис регистрации в ICQ только если у Вас нет возможности зарегистрироваться на <a href="http://www.icq.com/join/">официальном сайте</a>.<br/>
            Данный сервис не собирает информацию о зарегистрированных пользователях и не ведет никакой подобной статистики.<br/>
            При регистрации обязательно правильно указывайте email. Он будет использоваться в качестве Вашего логина. Так же на него придет ссылка для активации аккаунта, без активации регистрация завершена не будет.<br/>
        </p>

        {{ form_start(form, {'action': path('icq_registration'), 'method': 'POST'}) }}
            {{ form_row(form.reg_type, {'value': reg_type}) }}
            {{ form_row(form.csrf, {'value': csrf}) }}
            {{ form_row(form.gnm, {'value': gnm}) }}
            {{ form_row(form.first_name) }}
            {{ form_row(form.last_name) }}
            {{ form_row(form.email) }}
            {{ form_row(form.password) }}
            <img src="{{ path('icq_registration_pic', {'gnm_img': gnm_img}) }}" alt="код"/><br/>
            {{ form_row(form.captcha) }}
            {{ form_row(form.submit) }}
        {{ form_end(form) }}
    {% endif %}
{% endblock %}