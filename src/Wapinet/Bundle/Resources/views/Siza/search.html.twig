{% extends '::base.html.twig' %}

{% block title %}Поиск{% if form.search.vars.value %} - {{ form.search.vars.value }}{% endif %}{% endblock %}
{% block breadcrumbs %}
    {{wapinet_breadcrumbs({
    1: {'uri': path('downloads'), 'title': 'Развлечения'},
    2: {'uri': path('siza_index'), 'title': 'Бесплатные загрузки'}
    })}}
{% endblock %}

{% block content %}
    {{ form_start(form, {'action': path('siza_search'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
