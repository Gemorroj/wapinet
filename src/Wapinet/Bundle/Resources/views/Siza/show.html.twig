{% extends '::base.html.twig' %}

{% block title %}Бесплатные загрузки - {{ name }}{% endblock %}
{% block breadcrumbs %}
    {% set items = {1: {'uri': path('downloads'), 'title': 'Загрузки, развлечения'}} %}

    {% for item in breadcrumbs %}
        {% set key = loop.index + 1 %}
        {% set items = items|merge({ (key): {'uri': path('siza_index', {'q': item.query}), 'title': item.name }}) %}
    {% endfor %}

    {{ wapinet_breadcrumbs(items) }}
{% endblock %}

{% block content %}
    {{ content|raw }}

    <div class="pagerfanta">
        {{ pages|raw }}
    </div>
{% endblock %}
