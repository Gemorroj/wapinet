{% extends "::base.html.twig" %}

{% block title %}Конвертер в Unicode{% endblock %}
{% block menu %}
    {{wapinet_menu({
    1: {'uri': path('utilities'), 'title': 'Полезные WEB приложения'}
    })}}
{% endblock %}


{% block content %}
    Данный сервис позволяет перекодировать текст в кодировку <abbr title="Юникод (англ. Unicode) — стандарт кодирования символов, позволяющий представить знаки практически всех письменных языков">Unicode</abbr>.
    <br/>

    {{ form_start(form, {'action': path('unicode_index'), 'method': 'POST'}) }}
    {% if result is not null %}
        <label>Результат <textarea>{{ result }}</textarea></label>
    {% endif %}

    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
