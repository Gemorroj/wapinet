{% extends '::base.html.twig' %}

{% block title %}Погода - {{ countryName }}{% endblock %}
{% block breadcrumbs %}
    {{wapinet_breadcrumbs({
    1: {'uri': path('downloads'), 'title': 'Загрузки, развлечения'},
    2: {'uri': path('weather_country'), 'title': 'Погода'}
    })}}
{% endblock %}


{% block content %}
    {{ form_start(form, {'action': path('weather_city', {'country': country}), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
