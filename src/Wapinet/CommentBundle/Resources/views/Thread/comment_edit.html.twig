{#

 This file is part of the FOSCommentBundle package.

 (c) FriendsOfSymfony <http://friendsofsymfony.github.com/>

 This source file is subject to the MIT license that is bundled
 with this source code in the file LICENSE.

#}

<div class="fos_comment_comment_form_holder">
    {{ form_start(form, {'action': path('fos_comment_put_thread_comments', {'id': comment.thread.id, 'commentId': comment.id}), 'method': 'POST', 'attr': {'class': 'fos_comment_comment_edit_form'}}) }}
        {% block fos_comment_form_fields %}
            {{ form_errors(form) }}
            {{ form_errors(form.body) }}
            {{ form_widget(form.body) }}
            {{ form_widget(form._token) }}
        {% endblock %}

        <div class="fos_comment_submit">
            {% block fos_comment_form_submit %}
                <input data-inline="true" type="submit" value="Изменить" />
            {% endblock %}
            <a href="#" data-role="button" data-inline="true" class="fos_comment_comment_edit_cancel">Отмена</a>
        </div>

        <input type="hidden" name="_method" value="PUT" />

    {{ form_end(form) }}
</div>
