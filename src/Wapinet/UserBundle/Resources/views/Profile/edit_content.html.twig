{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'action': path('fos_user_profile_edit'), 'method': 'POST', 'class': 'fos_user_profile_edit'}) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="Обновить" />
    </div>
{{ form_end(form) }}
