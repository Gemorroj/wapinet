<a href="{{ path('file_view', {'id': event.variables.file.id}) }}">
    {% if event.variables.friend.isWoman %}
        Ваша подруга {{ event.variables.friend }} добавила новый файл {{ event.variables.file.originalFileName }} ({{ event.variables.file.file.size|wapinet_size }}).
    {% else %}
        Ваш друг {{ event.variables.friend }} добавил новый файл {{ event.variables.file.originalFileName }} ({{ event.variables.file.file.size|wapinet_size }}).
    {% endif %}
</a>
