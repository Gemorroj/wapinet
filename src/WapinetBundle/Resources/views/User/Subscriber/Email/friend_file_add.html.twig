{% extends '@FOSUser/Subscriber/Email/_layout.html.twig' %}

{% block content %}
    {% if friend.isFemale %}
        Ваша подруга <a href="{{ url('wapinet_user_profile', {'username': friend.username}) }}">{{ friend }}</a> добавила новый файл <a href="{{ url('file_view', {'id': file.id}) }}">{{ file.originalFileName }}</a> ({{ file.fileSize|wapinet_size }}).
    {% else %}
        Ваш друг <a href="{{ url('wapinet_user_profile', {'username': friend.username}) }}">{{ friend }}</a> добавил новый файл <a href="{{ url('file_view', {'id': file.id}) }}">{{ file.originalFileName }}</a> ({{ file.fileSize|wapinet_size }}).
    {% endif %}
{% endblock %}
