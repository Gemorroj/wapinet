{% extends 'User/Subscriber/Email/_layout.html.twig' %}

{% block content %}
    {% if friend.isFemale %}
        Вас удалила из друзей <a href="{{ url('wapinet_user_profile', {'username': friend.username}) }}">{{ friend }}</a>.
    {% else %}
        Вас удалил из друзей <a href="{{ url('wapinet_user_profile', {'username': friend.username}) }}">{{ friend }}</a>.
    {% endif %}
{% endblock %}
